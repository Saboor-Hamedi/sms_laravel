// const textarea = document.getElementById("name");

// // Function to auto-resize the textarea
// function autoResize() {
//     this.style.height = "auto"; // Reset height
//     this.style.height = this.scrollHeight + "px"; // Set height to scrollHeight
// }

// // Event listener for input to auto-resize
// textarea.addEventListener("input", autoResize);

// // Prevent default behavior of Enter key and allow Shift + Enter for new line
// textarea.addEventListener("keydown", function (event) {
//     if (event.key === "Enter" && !event.shiftKey) {
//         event.preventDefault(); // Prevent default Enter behavior
//         // The Livewire method will handle the submission
//     }
// });

// // Initial call to set the height based on the initial content
// autoResize.call(textarea);
